[project]
name = "mcp_fmi_ecc26"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.3",
    "pydantic>=2.12.0",
    "pytest>=8.4.2",
    "scipy>=1.16.2",
    "typer>=0.12.0",
]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
ecc26 = "mcp_fmi_ecc26.cli:main"